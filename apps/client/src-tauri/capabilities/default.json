{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Foundation client capability set",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-local-runtime"
  ]
}
